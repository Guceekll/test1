用户提问 → Agent 接收 → 查数据库/模型 → 分析 → 输出报告

用户输入 → Agent → 识别需要工具 → 调用 API → 输出结果

对话历史 (黑板)
└→ 今天东京天气 → 保存
└→ 用户问明天 → 自动补充“东京” → 输出

用户任务
 ├─ 分析 Agent → 查数据
 └─ 报告 Agent → 写报告
 → 合并输出

输入测试数据 → Agent → 输出结果 → 对比预期 → 测试通过
